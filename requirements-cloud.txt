# ═══ Ant's — dependências ENXUTAS para deploy na nuvem (free tier) ═══
# Só o necessário para subir a API, servir a interface e /health responder
# com os 23 módulos. Os aceleradores pesados (chromadb, sentence-transformers,
# torch, scikit-learn, playwright) são OPCIONAIS e ficam de fora — a colônia
# funciona 100% offline sem eles. Isso mantém a imagem leve e o build rápido,
# evitando estourar RAM/tempo de build dos planos gratuitos.

# Núcleo / API
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
httpx>=0.27
pydantic>=2.7
structlog>=24.1

# Percepção (import no boot: cálculo, imagem, OCR, documentos)
numpy>=1.26
sympy>=1.12
Pillow>=10.0
pytesseract>=0.3.10
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6.23
pymupdf>=1.24

# Memória bio-inspirada (agendador do ciclo de sono)
apscheduler>=3.10

# App Factory (CLI interna)
typer>=0.12
rich>=13.0
